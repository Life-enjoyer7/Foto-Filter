# Convolution Image Filtering (C)

Проект реализует **ручную свёртку (convolution)** для фильтрации изображений на языке C с использованием OpenCV и GTK.  
Позволяет применять различные фильтры (размытие, резкость, детектор границ, тиснение и др.) и тестировать производительность.

---

## Зависимости

Перед сборкой убедитесь, что установлены следующие библиотеки:

| Библиотека | Назначение | Команда для Ubuntu/Debian |
|------------|------------|---------------------------|
| OpenCV | загрузка/сохранение изображений, отображение | `sudo apt install libopencv-dev` |
| GTK 3.0 | диалоговое окно выбора файла | `sudo apt install libgtk-3-dev` |
| pkg-config | автоматическое получение флагов компиляции | `sudo apt install pkg-config` |

---

## Сборка и запуск (Makefile)

Проект использует `make` для сборки.

### Доступные цели

| Команда | Описание |
|---------|----------|
| `make run` | скомпилировать и запустить версию с GTK-диалогом |
| `make test` | запустить все тесты |
| `make clean` | удалить папку `build/` |
| `make help` | показать справку по командам |

### Пример сборки и запуска

```bash
# Клонирование репозитория
git clone https://github.com/Life-enjoyer7/Foto-Filter.git
cd Foto-Filter

# Сборка и запуск тестов
make test

# Сборка GUI версии
make run